# 🚀 دليل التشغيل الكامل - Tabibi

## الخطوة 1: التحضيرات الأولية

### تثبيت المكتبات
```bash
npm install
# أو إذا استخدمت pnpm
pnpm install
```

### إعداد Supabase
1. انتقل إلى [supabase.com](https://supabase.com)
2. أنشئ project جديد
3. انسخ عنوان URL و API Key
4. أضفهما في متغيرات البيئة

## الخطوة 2: متغيرات البيئة

أنشئ ملف `.env.local` (أو أضف لـ `.env`):
```env
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
DATABASE_URL=postgresql://user:password@host/database

# اختياري
NODE_ENV=development
```

## الخطوة 3: تشغيل قاعدة البيانات

إذا لم تقم بتشغيل الهجرة:
```bash
npm run db:push
```

## الخطوة 4: تشغيل التطبيق

### أثناء التطوير (الطريقة الموصى بها)
```bash
npm run dev
```
- السيرفر: http://localhost:3000
- تحديث تلقائي عند تغيير الملفات
- رسائل الخطأ التفصيلية في Console

### البناء والإنتاج
```bash
npm run build
npm run start
```

## الخطوة 5: الاختبار

### تسجيل حساب جديد
1. انتقل إلى http://localhost:3000
2. اختر "مريض" أو "طبيب"
3. ملء النموذج وإنشاء الحساب

### اختبار لوحة المريض
- تسجيل دخول كمريض
- عرض البيانات الصحية
- البحث عن الأطباء
- حجز موعد

### اختبار لوحة الطبيب
- تسجيل دخول كطبيب
- عرض المواعيد
- إدارة المرضى

## استكشاف الأخطاء

### المشكلة: "No available port found"
```bash
# تحديد منفذ مخصص
PORT=3001 npm run dev
```

### المشكلة: "Database connection error"
- تحقق من `DATABASE_URL` في `.env`
- تأكد من أن Supabase يعمل
- جرب: `npm run db:push`

### المشكلة: "TRPC connection error"
- تأكد من أن السيرفر يعمل على `http://localhost:3000`
- افتح Developer Tools (F12) وتحقق من Network
- جرب تحديث الصفحة

### المشكلة: "Module not found"
```bash
# أعد تثبيت المكتبات
rm -rf node_modules
npm install
```

## الأوامر المتاحة

```bash
npm run dev          # تطوير
npm run build        # بناء للإنتاج
npm run start        # تشغيل الإنتاج
npm run check        # التحقق من TypeScript
npm run format       # تنسيق الكود
npm run test         # اختبار الوحدات
npm run db:push      # هجرة قاعدة البيانات
```

## معلومات إضافية

- **الملفات الهامة**: انظر `SYSTEM_ARCHITECTURE.md`
- **خطة الاختبار**: انظر `TESTING_GUIDE.md`
- **هندسة النظام**: انظر `SYSTEM_ARCHITECTURE.md`

---

## 💡 نصائح مهمة

1. **استخدم Developer Tools** - اضغط F12 لرؤية السجلات والأخطاء
2. **تحقق من Network Tab** - لرؤية طلبات tRPC
3. **استخدم Console** - ستظهر رسائل تصحيح من v0 بشكل "[v0]"
4. **احفظ الملفات** - التحديث يحدث تلقائياً

## 🎯 التدفقات الأساسية

### تدفق المريض:
1. الصفحة الرئيسية (`/`)
2. تسجيل الدخول (`/auth/patient/login`)
3. لوحة التحكم (`/patient/dashboard`)
4. البحث عن طبيب (`/patient/search`)
5. حجز موعد

### تدفق الطبيب:
1. تسجيل الدخول (`/auth/professional/login`)
2. لوحة التحكم (`/doctor/dashboard`)
3. إدارة المواعيد

---

**الآن جاهز للتشغيل! 🎉**

اضغط على الأمر أعلاه وابدأ اختبار التطبيق.
