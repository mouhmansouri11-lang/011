## Tabibi - تطبيق حجز المواعيد الطبية في الجزائر

تطبيق ويب متكامل يربط المرضى مع المتخصصين الطبيين في الجزائر.

### الميزات الرئيسية:

- 🔐 نظام مصادقة آمن
- 🏥 إدارة المواعيد
- 📊 تتبع البيانات الصحية
- 🔍 البحث عن الأطباء
- 💊 إدارة الوصفات الطبية
- 📋 تقارير المختبرات

### البدء السريع:

```bash
# 1. التثبيت
npm install
# أو
pnpm install

# 2. التشغيل
npm run dev

# 3. الفتح
http://localhost:3000
```

### التكنولوجيا المستخدمة:

- React 18
- TypeScript
- tRPC + Express
- Supabase
- Tailwind CSS
- Shadcn/UI

### هيكل المشروع:

```
├── client/src/
│   ├── pages/           # الصفحات الرئيسية
│   ├── components/      # المكونات المعاد استخدامها
│   ├── contexts/        # Context API
│   ├── utils/           # الدوال المساعدة
│   └── App.tsx          # التطبيق الرئيسي
├── server/
│   ├── _core/           # إعدادات السيرفر
│   ├── routers.ts       # tRPC Routers
│   └── db.ts            # قاعدة البيانات
├── drizzle/
│   └── schema.ts        # ORM Schema
└── vite.config.ts       # تكوين Vite
```

### الملفات التوثيقية:

- **DEPLOYMENT_FIXES_SUMMARY.md** - ملخص الإصلاحات المطبقة
- **BUILD_FIXES.md** - شرح مشاكل البناء والحلول
- **KNOWN_ISSUES.md** - النقائص المعروفة والخطوات اللاحقة
- **STARTUP_GUIDE.md** - دليل بدء التشغيل
- **SYSTEM_ARCHITECTURE.md** - هندسة النظام

### الإعدادات المطلوبة:

تأكد من إنشاء ملف `.env.local`:

```env
NODE_ENV=development
PORT=3000
VITE_API_URL=http://localhost:3000/api
JWT_SECRET=your_secret_key
```

### أوامر مفيدة:

```bash
npm run dev          # تشغيل بوضع التطوير
npm run build        # بناء الإنتاج
npm run start        # تشغيل الإنتاج
npm run check        # فحص الأنواع
```

### استكشاف الأخطاء:

إذا واجهت أخطاء:

1. اقرأ **KNOWN_ISSUES.md**
2. تحقق من console في المتصفح
3. تأكد من تثبيت الحزم: `npm install`
4. جرب تنظيف cache: `rm -rf node_modules pnpm-lock.yaml && npm install`

### الدعم والمساعدة:

- اقرأ الملفات التوثيقية في المشروع
- تحقق من الأخطاء في console
- أرسل الأخطاء للإصلاح

---

**تم إنشاؤها بـ React + TypeScript + tRPC + Supabase**

استمتع بالتطبيق! 🚀
